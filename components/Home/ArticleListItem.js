import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ArticleListItem = () => {
  return (
    <View>
      <Text>ArticleListItem</Text>
    </View>
  );
};

export default ArticleListItem;

const styles = StyleSheet.create({});
